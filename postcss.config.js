// Empty PostCSS config — Tailwind v4 is handled by @tailwindcss/vite plugin,
// which doesn't need PostCSS. This file exists to prevent Vite from discovering
// a postcss.config in a parent directory.
export default {};
